<Window x:Class="TanukiTarkovMap.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TanukiTarkovMap"
        Width="280" Height="360"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True">

    <Border Background="#1a1a2e" CornerRadius="12" BorderBrush="#3a3a5e" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo and Title -->
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image x:Name="LogoImage"
                       Source="/Resources/icon.png"
                       Width="120" Height="120"
                       RenderTransformOrigin="0.5,0.5"/>
                <TextBlock Text="TanukiTarkovMap"
                          FontSize="20"
                          FontWeight="SemiBold"
                          Foreground="White"
                          HorizontalAlignment="Center"
                          Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Status Text -->
            <TextBlock Grid.Row="1"
                      x:Name="StatusText"
                      Text="시작하는 중..."
                      FontSize="12"
                      Foreground="#a0a0b0"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,18"/>

            <!-- Progress Bar (다운로드 시에만 표시) -->
            <Grid x:Name="ProgressBarContainer" Grid.Row="2" Height="4" Visibility="Collapsed">
                <Border Background="#2a2a4e" CornerRadius="2"/>
                <Border x:Name="ProgressFill" CornerRadius="2" Width="0" HorizontalAlignment="Left">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#6366f1" Offset="0"/>
                            <GradientStop Color="#8b5cf6" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
